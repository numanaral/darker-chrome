{
  "name": "darker-chrome",
  "version": "0.0.1",
  "description": "Chrome extension that enables dark mode on all websites and remembers your preference for each site.",
  "scripts": {
    "pack": "tsx scripts/pack.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.19.2",
    "tsx": "^4.0.0",
    "typescript": "^5.3.0"
  }
}
